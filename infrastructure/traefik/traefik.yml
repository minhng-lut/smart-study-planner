api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"
  metrics:
    address: ":8082"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false

ping: {}

metrics:
  prometheus:
    entryPoint: metrics

